<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.habitual.demo.carouselImage.mapper.CarouselImageMapper">

    <insert id="insert" parameterType="com.habitual.demo.carouselImage.entity.CarouselImageEntity" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO carousel_image (
        <trim suffixOverrides=",">
            <if test="id != null">id,</if>
            <if test="createBy != null and createBy != ''">create_by,</if>
            create_time,
            <if test="updateBy != null and updateBy != ''">update_by,</if>
            update_time,
            <if test="remark != null and remark != ''">remark,</if>
            <if test="url != null and url != ''">url,</if>
        </trim>
        ) VALUES (
        <trim suffixOverrides=",">
            <if test="id != null">#{id},</if>
            <if test="createBy != null and createBy != ''">#{createBy},</if>
            #{createTime},
            <if test="updateBy != null and updateBy != ''">#{updateBy},</if>
            #{updateTime},
            <if test="remark != null and remark != ''">#{remark},</if>
            <if test="url != null and url != ''">#{url},</if>
        </trim>
        )
    </insert>

    <update id="update" parameterType="com.habitual.demo.carouselImage.entity.CarouselImageEntity">
        UPDATE carousel_image
        <set>
            <trim suffixOverrides=",">
                <if test="createBy != null and createBy != ''">create_by = #{createBy},</if>
                create_time = #{createTime},
                <if test="updateBy != null and updateBy != ''">update_by = #{updateBy},</if>
                update_time = #{updateTime},
                <if test="remark != null and remark != ''">remark = #{remark},</if>
                <if test="url != null and url != ''">url = #{url},</if>
            </trim>
        </set>
        WHERE id = #{id}
    </update>

    <delete id="delete">
        DELETE FROM carousel_image
        WHERE id = #{id}
    </delete>

    <select id="getTotalCount">
        SELECT COUNT(*) FROM carousel_image
    </select>

    <select id="selectByPage" parameterType="com.habitual.demo.carouselImage.entity.dto.CarouselImagePageDto" resultType="com.habitual.demo.carouselImage.entity.CarouselImageEntity">
        SELECT
        id, create_by, create_time, update_by, update_time, remark,
        url
        FROM carousel_image
        WHERE 1=1
        <if test="createBy != null and createBy != ''">
            AND create_by = #{createBy}
        </if>
        <if test="updateBy != null and updateBy != ''">
            AND update_by = #{updateBy}
        </if>
        <if test="remark != null and remark != ''">
            AND remark = #{remark}
        </if>
        ORDER BY create_time DESC
        LIMIT #{offset}, #{pageSize}
    </select>

</mapper>
